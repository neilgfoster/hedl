{
  "date": "2026-05-29",
  "completed": [
    "WORK-0017: phase definitions + Phase 0->1 transition (PR #20).",
    "WORK-0016: .hedl-tier no hardcoded absolute path (PR #21).",
    "WORK-0019: startup.sh printf RCE verified false positive; regression guard (PR #22).",
    "WORK-0020: install.py SKILL_ROOT/repo containment on tier projections (PR #23).",
    "WORK-0021: am_i_done.py [verify] executable allow-list. Audit confirmed a REAL vuln (no allow-list on cmd[0]); fixed with a bare-name allow-list (default pytest/mypy/ruff/npm/pnpm/make + [gate] allowed_commands), interpreter/forwarder denylist, shell-metachar rejection, and cwd containment. Two-cycle security review caught 3 bypasses in the first attempt.",
    "WORK-0037: ADOPTERS.md (root) + README links. First adopter = Hedl (dogfood). Adversarial panel: historian PASS, scope-auditor FAIL (goal-displacement); FAIL overridden by operator via /change-direction, reason recorded on the work item.",
    "WORK-0038: Dependabot checkout v5.0.1->v6.0.2 (PR #39, merged b29a558). SHA verified vs upstream tag; v6 breaking changes no-impact. PR body rewritten to satisfy the Hedl template (gate's template check fetches live body); branch updated; gate+CodeQL green.",
    "WORK-0040: Dependabot ruff 0.15.14->0.15.15 uv (PR #42, merged 0531caa). Patch; preview-gated rules off; ruff clean confirmed under 0.15.15 in CI.",
    "WORK-0039: ruff pip #41 closed as superseded by #42 (identical pyproject edit; auto-closed on merge).",
    "WORK-0034: /iterate now drives end-to-end through to an operator-ready PR (ADR-025). Adversarial review caught the ADR-025 branch-protection-assertion requirement + a premature-completion/stuck-recovery BLOCKING; both fixed. Delivered end-to-end from one /iterate (AC6).",
    "WORK-0024: state_backend moved to hedl.toml (ADR-022 implemented, operator chose hedl.toml over superseding). install.py schema 1->2 migration + hardened TOML writer (injection guard, raise-on-corrupt, crash-safe ordering). Adversarial review caught 2 BLOCKING ordering/atomicity bugs + a TOML-injection gap; all fixed over 2 cycles. WORK-0032/0033 ACs reconciled.",
    "WORK-0026: team-tier description reconciled across 5 sources (tiers.json/README/tiers.md/team-tier.md/getting-started.md) to one truth; GH Issues backend correctly framed as read-only-today (write-back planned WORK-0012). AC3 guard test added across all 3 tiers, derived from real projections. Review caught a 4th drift source (getting-started.md) + 3 test-coverage gaps; all fixed.",
    "WORK-0028: count-drift fix + check_doc_generated_facts gate detector (deterministic, fence-aware). Review caught a 4th stale count (commands.md '(core 6)') the first regex missed + a fragile ADR-template denylist; fixed via parenthetical pattern + code-fence-aware counting. 354 tests.",
    "WORK-0022: install.py guards — malformed .hedl-tier (status/doctor no longer traceback; install WARNs) + tiers.json includes cycle/unknown-tier/depth detection under TiersConfigError. Audit-first; both bugs real. Review caught 3 more crash paths (empty/unknown tier) + the deep-recursion 'no stack overflow' AC; all fixed. 372 tests.",
    "WORK-0023: release.py --current-version validation. Non-canonical input now yields a JSON error envelope + exit 1, not a traceback. Review caught a BLOCKING `$`-trailing-newline bypass and silent leading-zero coercion; fixed with \\Z + canonical no-leading-zero regex. 376 tests.",
    "WORK-0005: existential-challenger promoted to the 8th named agent + dispatch wiring (live+template) + tiers.json projection + phase-complete (ADR-013) + 8/18 counts. The WORK-0028 doc-facts detector forced the agent-count doc updates (dogfood). Review caught two adopter-breaking source/template divergences (tiers.json projection, work-state dispatch-rules template); both fixed, fresh-install-verified. 379 tests.",
    "WORK-0042: install.py ships a .gitignore block protecting ADR-005 local-only insights artefacts. Review corrected a false ADR-005 citation (ADR-005 is /contribute privacy, not insights-never-commit), completed coverage (metrics.json/proposals), guarded a directory-read crash, and dogfood-aligned Hedl own .gitignore. 386 tests."
  ],
  "decisions": [
    "WORK-0021 audit-first outcome = FIX (real vuln), the counterpart to WORK-0019's guard-only false positive. Both recorded under .work/insights/.",
    "Default allow-list = ticket's set incl. make/npm (operator's informed choice; they run repo-committed scripts, accepted).",
    "Key reframing from review: the allow-list is DEFENSE IN DEPTH, not a complete RCE control — pytest(conftest)/make/npm run committed content; the real untrusted-PR control is GitHub fork-approval. Documented in references/tiers.md and the code comment.",
    "cmd[0] must be a bare name (closes abs-path bypass without breaking .venv/node_modules PATH resolution); denylist enforced in two layers.",
    "WORK-0037 scope-auditor FAIL (net-new artefact vs phase-1 anti-goal-displacement) overridden: operator deems a hand-maintained ADOPTERS.md a justified OSS convention, not process accretion."
  ],
  "next_item": "WORK-0009",
  "blockers": [],
  "notes": "WORK-0042 (high) delivered via /iterate. active_item null. Flagged for /phase-complete: 'insights are local-only' lacks its own ADR (ADR-005 is /contribute privacy); whether to formalise. Source/template divergence theme continues (WORK-0025). Next per priority: WORK-0009 (Principle 4 in CLAUDE.md) or WORK-0025."
}
